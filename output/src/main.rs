
fn main() {

let mut buf = vec![0_i32];
let mut pointer = 0;
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
(0..1).for_each(|_| get_char(&mut buf, pointer));
(0..1).for_each(|_| increment_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..2).for_each(|_| pointer += 1);
(0..4).for_each(|_| increment_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer += 1);
(0..8).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..3).for_each(|_| pointer += 1);
}
(0..1).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..2).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
}
(0..5).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
}
(0..3).for_each(|_| pointer += 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..2).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..3).for_each(|_| increment_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
}
}
(0..1).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..12).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..2).for_each(|_| pointer += 1);
}
(0..1).for_each(|_| pointer += 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| increment_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..2).for_each(|_| pointer += 1);
}
(0..5).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..2).for_each(|_| pointer += 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..1).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| pointer += 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
(0..2).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..2).for_each(|_| pointer += 1);
}
(0..2).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..2).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
(0..2).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..2).for_each(|_| pointer += 1);
}
(0..2).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..2).for_each(|_| pointer -= 1);
(0..1).for_each(|_| increment_value(&mut buf, pointer));
(0..2).for_each(|_| pointer += 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
}
(0..1).for_each(|_| pointer -= 1);
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| put_char(&buf, pointer));
while buf[pointer] != 0 {
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
}
(0..1).for_each(|_| pointer -= 1);
(0..1).for_each(|_| decrement_value(&mut buf, pointer));
(0..1).for_each(|_| get_char(&mut buf, pointer));
(0..1).for_each(|_| increment_value(&mut buf, pointer));
}


println!();
}


fn fill_array(buf: &mut Vec<i32>, pointer: usize) {
    while buf.len() <= pointer {
        buf.push(0);
    }
}

fn increment_value(buf: &mut Vec<i32>, pointer: usize) {
    fill_array(buf, pointer);
    buf[pointer] += 1;
}

fn decrement_value(buf: &mut Vec<i32>, pointer: usize) {
    fill_array(buf, pointer);
    buf[pointer] -= 1;
}

fn put_char(buf: &Vec<i32>, pointer: usize) {
    print!("{}", (buf[pointer] as u8) as char);
}

fn get_char(buf: &mut Vec<i32>, pointer: usize) {
    fill_array(buf, pointer);
    println!();
    println!("Waiting for user input!");
    let term = console::Term::stdout();
    let ch = term.read_char().unwrap();
    buf[pointer] = ch as i32;
}

